# Copy to .env.local to use your tldraw licence key when building locally (never commit .env.local).
VITE_TLDRAW_LICENSE_KEY=
# Live rooms: URL of the deployed sync-worker (e.g. https://weekly-presentation-sync.<you>.workers.dev)
VITE_SYNC_URL=

{
	"name": "presentations-site",
	"private": true,
	"type": "module",
	"scripts": {
		"export": "node scripts/export-decks.mjs",
		"dev": "npm run export && vite",
		"build": "npm run export && vite build",
		"preview": "vite preview"
	},
	"dependencies": {
		"@tldraw/sync": "^5.4.2",
		"qrcode-generator": "^2.0.4",
		"react": "^19.3.0",
		"react-dom": "^19.3.0",
		"tldraw": "^5.4.2"
	},
	"devDependencies": {
		"@vitejs/plugin-react": "^6.1.1",
		"vite": "^8.3.1"
	}
}

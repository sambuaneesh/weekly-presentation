{
	"name": "weekly-presentation-site",
	"private": true,
	"type": "module",
	"scripts": {
		"export": "node scripts/export-deck.mjs ../weekly-presentation.tldraw src/deck.json",
		"dev": "npm run export && vite",
		"build": "npm run export && vite build",
		"preview": "vite preview"
	},
	"dependencies": {
		"react": "^19.3.0",
		"react-dom": "^19.3.0",
		"tldraw": "^5.4.2"
	},
	"devDependencies": {
		"@vitejs/plugin-react": "^6.1.1",
		"vite": "^8.3.1"
	}
}
